name: My Workflow

on: [push, workflow_dispatch]

jobs:
  my_job:
    runs-on: ubuntu-latest
    steps:
      - name: Step 1
        run: echo "Hello, world!"
      - name: Step 2
        run: ls -lr ${{ runner.temp }}/_github_workflow
